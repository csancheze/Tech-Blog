{{#each posts as |post|}}

<div>
    <h2>
        <a href="post/{{post.id}}">{{post.title}}</a> <span>{{post.user.name}} on {{format_date post.date_created}}</span>
    </h2>
    <p>
        {{post.content}}
    </p>

</div>

{{/each}}