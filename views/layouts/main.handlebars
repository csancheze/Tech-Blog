<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@200&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/normalize.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    
    
</head>
<body class="bg-dark">
  <header class="bg-secondary container-fluid mt-5 mb-2" style="display:flex; justify-content:space-between; flex-wrap:wrap">
      <h1 class="">
        <a class="display-2" href="/">Tech Blog</a>
      </h1>
      <ul class="nav">
          {{#if logged_in}}
          <li class="nav-item pt-2 pb-2">
          <a class=" nav-link display-5 btn-dark " href="/dashboard">Dashboard</a>
          </li> 
          <li class="nav-item">
          <button class="btn btn-secondary m-1" style="position: absolute; top:0; right:0"id="logout">Logout</button>
          </li>
          {{else}}
          <li class="nav-item pt-2 pb-2" >
          <a class="nav-link display-5 btn-dark" href="/login">Login</a>
          </li>
          {{/if}}
      </ul>
  </header>
  <main class="container pt-5">
        {{{ body }}}
  </main>
  <footer>
   
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/js/bootstrap.bundle.min.js"></script>
  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if}}
      
</body>
</html>